"""Reusable components for unsupervised equivariant graph representations.

The package root is intentionally lightweight. Import concrete pieces from
`uer.datasets`, `uer.models`, `uer.trainers`, or `uer.factories` so optional
dependencies and registry side effects stay localized.
"""

__all__: list[str] = []
